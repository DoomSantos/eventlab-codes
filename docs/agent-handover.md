# Agent handover — DSR EventLab

## LAST COMPLETED

- Railway production API live
- Public `leaderboard.html` unveiled (24h + live)
- Friend Windows zip built with API preloaded
- Invite minting via `POST /v1/admin/invites` + `TIMING_ADMIN_TOKEN`

## NEXT

Public timing stays “Soon” while CD builds time-attack EventLab starts.  
Private lab: `board-lab.html`. Friend collector + Railway API still live.
